var slider = tns({
    container: '.slider-body',
    prevButton: '.slider-left',
    nextButton: '.slider-right',
    navPosition: 'bottom'
})

var slider2 = tns({
    container: '.sliderCards-body',
    nav: false,
    prevButton: '.sliderCards-left',
    nextButton: '.sliderCards-right',
    slideBy: 'page'
})